# Database (provided by Railway PostgreSQL plugin)
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# JWT Secrets (generate strong random strings)
JWT_SECRET="change-me-in-production"
JWT_EXPIRATION="4h"
JWT_REFRESH_EXPIRATION="7d"

# Server
PORT=4100
NODE_ENV=production

# Application URL (used for OAuth callbacks)
APP_URL="https://qsasset.vercel.app"

# CORS - comma-separated allowed origins
CORS_ORIGIN="https://reconapm.com,https://www.reconapm.com"

# OAuth — Google (optional, leave empty to disable)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# OAuth — Microsoft / Azure AD (optional, leave empty to disable)
MICROSOFT_CLIENT_ID=""
MICROSOFT_CLIENT_SECRET=""

# OAuth callback URL (override if using custom domain)
OAUTH_CALLBACK_URL=""

# AI Engine (Gemma 4 via vLLM or Ollama)
AI_ENABLED=false
AI_BASE_URL="http://localhost:11434/v1"
AI_MODEL="gemma3:4b"
AI_API_KEY="not-needed"
AI_MAX_TOKENS=4096
AI_TEMPERATURE=0.3

# Notifications — SMTP (optional)
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""

# Notifications — Webhook (optional)
ALERT_WEBHOOK_URL=""
